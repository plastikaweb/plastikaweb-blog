---
const { bg = 'primary', text = 'primary', size = 'md' } = Astro.props;
---

<button
  class={`cursor-pointer bg-${bg}-100 text-${text}-800 px-2 py-1 rounded hover:bg-${bg}-800 hover:text-${bg}-100 transition-colors duration-200`}
>
  <slot />
</button>
